  /statistics {
    /categories {
      <%- @statistics_categories.sort_by { |cat| cat['rank'] }.each do |cat| -%>
      /<%= cat['name'] %> { /glob "<%= cat['glob'] %>" }
      <%- end -%>
    }
  }
