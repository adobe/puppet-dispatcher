
  /sessionmanagement {
    /directory "<%= @sessionmanagement_directory %>"
    /encode "<%= @sessionmanagement_encode %>"
    /header "<%= @sessionmanagement_header %>"
    /timeout "<%= @sessionmanagement_timeout %>"
  }
